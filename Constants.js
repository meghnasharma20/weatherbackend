const DB_MODEL_REF = {
    NEWS : 'news',
    WEATHER : 'weather',
  };

  const STATUS_CODE = {
    ERROR: 0,
    SUCCESS: 1,
  };

  const PAGINATION = {
    PAGE_LIMIT : 10,
  }

  module.exports = Object.freeze({
    APP_NAME: "weatherapp",
    STATUS_CODE,
    DB_MODEL_REF,
    PAGINATION


  });
  // ========================== Export Module End ============================
